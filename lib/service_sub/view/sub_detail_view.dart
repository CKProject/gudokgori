import 'package:flutter/material.dart';

class SubDetailView extends StatelessWidget {
  const SubDetailView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
